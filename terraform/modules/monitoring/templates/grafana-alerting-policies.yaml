# Grafana 통합알림 알림정책(루트) — 모든 알림을 Slack contact point 로 라우팅.
# group_by 로 인스턴스/알림별 묶고, 5분 간격 반복(해소 전까지). MVP 단일 정책.
apiVersion: 1

policies:
  - orgId: 1
    receiver: slack
    group_by: ["alertname", "grafana_folder"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
